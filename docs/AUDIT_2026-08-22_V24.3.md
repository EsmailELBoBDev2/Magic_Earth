# CairoDrive v24.3 drive-test ready — 2026-08-22

Includes all v24.2 deep-audit hardening:
- exact Magic Lane 1.9.0 Coordinates/Path/Rgba wrapper construction;
- route reads, terrain inspection, traffic rendering and roadblock application on GEM SDK thread;
- stale Google route response rejection after reroute;
- corrected traffic interval/jam-run weighting;
- bounded dense route geometry (12 m target, 96 point/native-call ceiling);
- Google typed-search debounce + short negative cache + auth latches;
- freshness/accuracy-aware location selection;
- Free Drive follow-position gating, z15/z16 hysteresis, bounded cache/retries/cancellation;
- bounded native path/queue/object lifecycles;
- exact-Dex Magic Lane runtime-surface verification.

v24.3 additionally adds drive observability without frame hooks:
- asynchronous persistent feature log mirroring;
- app-specific external log directory;
- daily rotation + ~8 MiB rollover + ~3 day retention;
- 30-second CPU/PSS/native heap/Java heap/battery/temperature/thermal samples;
- updated `watch_drive.sh` and `pull_logs.sh` for current v24.3 markers;
- post-drive meminfo/cpuinfo/gfxinfo/battery/thermal snapshot and tar.gz export.

The v24.2 installer bug where the legitimate Places auth anchor appears twice is fixed.
